# CloudWatch module (Phase 2)

Planned resources:

- Dashboard: ALB request count / 5xx, ASG CPU, RDS CPU / free storage
- Alarms: `UnHealthyHostCount`, high CPU, RDS storage threshold
- SNS topic optional for alarm notifications (email)

Wire alarm ARNs into runbooks in project README.
