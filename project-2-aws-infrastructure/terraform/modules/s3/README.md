# S3 static assets module (Phase 2)

Planned resources:

- S3 bucket for static content (block public access by default)
- Bucket policy for CloudFront or ALB-origin access (choose one pattern)
- Optional versioning and lifecycle rules for lab cost control

Expected outputs: `bucket_name`, `bucket_arn`
